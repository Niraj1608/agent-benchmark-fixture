# Task T03-Tests: Add Unit Tests

## Problem Description

The string utility functions lack proper unit test coverage. Write comprehensive tests for the string manipulation functions.

## Expected Behavior

- Test coverage for all public functions in `src/string_utils.py`
- Tests for edge cases (empty strings, special characters, etc.)
- Tests should follow pytest conventions

## Acceptance Criteria

1. Tests for `reverse_string()` function
2. Tests for `capitalize_words()` function
3. Tests for `remove_whitespace()` function
4. Edge case coverage (empty strings, null inputs)
5. All tests pass

## Priority
Medium

## Labels
tests, unit-tests, coverage
